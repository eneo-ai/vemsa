# Review Agent pilot

This temporary documentation-only change verifies the Review Agent installation
for `eneo-ai/tolka` without changing application behavior.

The pilot succeeds when a top-level `/review` comment receives an acknowledgement
reaction and the resulting review is published against this pull request's exact
head commit.

This pull request is a disposable test fixture. Keep it as a draft and close it
without merging after validation.
